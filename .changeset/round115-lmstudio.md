---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
'mcp-agentgate-config-convert': minor
---

Discover LM Studio MCP configs (`~/.lmstudio/mcp.json`, Cursor-style `mcpServers` notation, same path on every platform) and support `lmstudio` as a source/target in `config convert`.
