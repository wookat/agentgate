---
'mcp-agentgate-config-convert': minor
'mcp-agentgate': minor
---

`config convert` supports `amazonq` (Amazon Q Developer, standard `mcpServers` notation at `.amazonq/mcp.json`) as source and target.
