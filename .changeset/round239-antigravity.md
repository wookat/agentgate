---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
---

Google Antigravity client support: discover global `~/.gemini/config/mcp_config.json` and workspace `.agents/mcp_config.json` MCP configs (serverUrl normalized), classify `.agents/hooks.json` / `~/.gemini/config/hooks.json` hook commands (AG-SK-003), and scan workspace rules `.agents/rules/*.md` incl. legacy `.agent/rules` (AG-SK-001).
