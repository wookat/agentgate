---
"mcp-agentgate-core": patch
"mcp-agentgate": patch
---

Advisory MCPA-2026-0095: @copilot-mcp/apex (npm) postinstall dropper — remote loader.sh piped into zsh plus an unverified staged binary, impersonating the @copilot-mcp scope (108 → 109).
