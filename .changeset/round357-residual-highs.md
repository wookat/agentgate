---
'mcp-agentgate-core': patch
---

Residual-high precision: AG-CL-001 skips versioned/vendored gitleaks configs (`gitleaks-v8.30.1.toml`), grades `test-*.sh`-convention files and local-issuer Supabase anon JWTs (`"iss":"supabase-local"`) low; AG-SS-001 recognizes `restrict*` identifiers/imports near a metadata address as defensive context.
