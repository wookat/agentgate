---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
---

Copilot custom agents: `.github/agents/*.md` agent profiles with an `mcp-servers` frontmatter map (official Copilot CLI / cloud agent custom-agent format) are discovered and their servers run through the full config rule set and advisory checks — the servers start for anyone who runs the agent.
