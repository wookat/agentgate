---
'mcp-agentgate-core': minor
'mcp-agentgate-config-convert': minor
'mcp-agentgate': minor
---

Discover Amp (Sourcegraph) MCP configs — the `amp.mcpServers` key in `~/.config/amp/settings.json` and workspace `.amp/settings.json` — and support `amp` in `config convert` (14 clients total).
