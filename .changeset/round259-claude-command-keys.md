---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
---

AG-SK-003 now classifies the Claude Code settings keys that run a command through the system shell automatically — `apiKeyHelper`, `awsAuthRefresh`, `awsCredentialExport`, and `statusLine.command` — with the shared dangerous-command classifier.
