---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
---

Qwen Code extensions: `qwen-extension.json` manifests (project root + installed under `~/.qwen/extensions/<name>/`) are discovered and their `mcpServers` run through the full config rule set and advisory checks — extensions start these servers automatically for anyone who installs them.
