---
'mcp-agentgate-config-convert': minor
'mcp-agentgate': minor
---

`config convert` supports `antigravity` (Google Antigravity): parse/render `.agents/mcp_config.json` / `~/.gemini/config/mcp_config.json`, emitting remote servers with the official `serverUrl` field.
