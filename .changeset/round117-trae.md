---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
'mcp-agentgate-config-convert': minor
---

Discover Trae (ByteDance) project-level MCP configs (`.trae/mcp.json`, standard `mcpServers` notation) and support `trae` as a source/target in `config convert`.
