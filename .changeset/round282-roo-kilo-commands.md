---
"mcp-agentgate-core": minor
"mcp-agentgate": minor
---

Skill scanning covers Roo Code project slash commands (`.roo/commands/*.md`) and Kilo Code's newer command location (`.kilo/commands/*.md`) — poisoning and load-time command checks (AG-SK-001/003) now run on both.
