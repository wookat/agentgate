---
'mcp-agentgate-core': minor
'mcp-agentgate': minor
---

Scan Roo Code project custom modes (`.roomodes`, YAML or legacy JSON) for prompt-injection/poisoning (AG-SK-001) — a mode's roleDefinition/customInstructions text is injected into the system prompt for every request in that mode.
