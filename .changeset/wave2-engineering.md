---
'agentgate': minor
'@agentgate/core': minor
---

Wave 2 engineering maturity: `--ignore` globs and `--debug` diagnostics for `scan`, strict exit-code contract (fatal explicit-config errors exit 2), unsupported lockfileVersion rejection with clear guidance, frozen lockfile v1 spec, and hardened supply chain (SHA-pinned actions, npm provenance release pipeline, CI dogfooding).
