---
'mcp-agentgate': patch
---

Table output wraps long file paths in the Target column mid-word instead of truncating them with an ellipsis, so the full path is always visible (messages still wrap on word boundaries).
