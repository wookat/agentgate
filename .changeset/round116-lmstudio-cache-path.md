---
'mcp-agentgate-core': patch
'mcp-agentgate': patch
---

Also discover LM Studio's `~/.cache/lm-studio/mcp.json` — current builds write the MCP config there (on macOS and Windows too) even though the documented location is `~/.lmstudio/mcp.json`.
