{
  "name": "@agentgate/advisory-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker serving the AgentGate MCP advisory database",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "npm run build:data && node --test test/*.test.mjs",
    "dev": "npm run build:data && wrangler dev",
    "deploy": "npm run validate && npm run build:data && wrangler deploy"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "wrangler": "^4.0.0"
  }
}
